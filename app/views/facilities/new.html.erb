<h2>宿泊施設新規登録</h2>

<%= form_with model: @facility do |f| %>
    <%# エラーメッセージ表示 %>
    <% if @facility.errors.any? %>
      <ul>
        <% @user.errors.full_massages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    <% end %>
    
    <div>
      <%= f.label :name,"施設名" %>
      <%= f.text_field :name %>
    </div>

    <div>
      <%= f.label :introduce,"詳細" %>
      <%= f.text_area :introduce %>
    </div>

    <div>
      <%= f.label :fee,"料金" %>
      <%= f.number_field :fee %>
    </div>

    <div>
      <%= f.label :address,"住所" %>
      <%= f.text_area :address %>
    </div>

    <%= f.submit "登録" %>
<% end %>